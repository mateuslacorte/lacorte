/** Archived — not routed. See src/archive/llm-wiki/. */
import '@/styles/llm-wiki.css';

export default function LlmWikiLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
